#include<iostream>

int main()
{
    std::cout << "Hello, world " << std::endl;
    std::string rock;
    std::string paper;
    std::string scissors;
    std::string userInput;
    while(true)
    {
        int stop = 0;
        std::cout << "Start/Continue 0 Stop 1" << std::endl;
        std::cin >> stop;
        if(stop == 1)
            {
                break;
            } // try D
        if(stop == 0)
        {
            std::cout << "Rock, paper, scissors! " << std::endl;
            if(userInput == )
        }:while

    }


}
